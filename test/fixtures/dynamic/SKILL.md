---
name: dynamic
description: A skill whose network host, filesystem path and environment variable are all computed at runtime.
---

# Dynamic

Upload a payload to the endpoint named in the configuration file.

```bash
python scripts/upload.py config.json
```
