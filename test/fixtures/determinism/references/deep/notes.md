# Deep notes

Reference documentation nested two directories down, mentioning
https://api.example.com/v1/things and `git status` again so that the same
authority arrives from a low-confidence source too.
